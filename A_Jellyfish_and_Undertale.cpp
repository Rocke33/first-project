#include <bits/stdc++.h>
using namespace std;

void rocke() {
    int a, b, n; cin >> a >> b >> n;
    vector<int>v(n);
    int sum=0;
    for(int i=0;i<n;i++){
        cin>>v[i];
        sum+=v[i];
    }
    while(b--){
        
    }
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        rocke();
    }
    return 0;
}